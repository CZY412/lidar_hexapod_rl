"""CLI entry points for the EA2 supervised-learning pipeline.

Each script is a thin wrapper: it parses arguments and delegates to the library
code under ``sl/`` so that the logic stays importable (and therefore testable).
"""
